class Profile {
  final String contract, name, image, id, description, bio;
  Profile(
      {this.contract,
      this.name,
      this.image,
      this.id,
      this.description,
      this.bio});
}
